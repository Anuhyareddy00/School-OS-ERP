# FastAPI backend env

# Optional: set to enable PostgreSQL + Prisma mode
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/school_os

# JWT secret for auth token signing
JWT_SECRET=change_me_to_a_long_random_secret

# Optional CORS origins (comma separated handled in code if extended)
CORS_ORIGIN=http://localhost:3000
